# Day--2-Task-Frontend
Components &amp; Props

Tasks:

Create a reusable card component
(You can use ANY universal fields like Title, Description, Category, etc.)
Display 3–5 cards using props
Customize each with different dummy data
(e.g., item name, summary, tags, etc.)
